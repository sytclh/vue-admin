import { version } from "../../package.json";
export const settingConfig: ServerConfigs = {
  version,
  title: "message.title",
  fixedHeader: true,
  hideTabs: false,
  hideSideBar: false,
  keepAlive: true,
  lang: "zh-cn",
  layout: "vertical",
  theme: "default",
  grey: false,
  weak: false,
};
